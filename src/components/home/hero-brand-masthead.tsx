import { LogoLockup } from "@/components/shared/logo-lockup";

export function HeroBrandMasthead() {
  return (
    <div className="hero-brand-masthead">
      <LogoLockup variant="hero" layout="stacked" />
    </div>
  );
}
